{
  "mcpServers": {
    "radarr-sonarr": {
      "command": "bash",
      "args": ["-c", "set -a && source .env 2>/dev/null && set +a && exec $HOME/.local/share/dulce-jelly-mcp/bin/python -m radarr_sonarr_mcp.server"],
      "env": {
        "NAS_IP": "localhost"
      }
    }
  }
}
