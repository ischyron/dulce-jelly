# CCC Preflight Scripts Configuration
# Copy this file to .env and update with your actual paths

# Directory where log files will be written
# CCC (running as root) will write to <LOG_DIR>/<script-name>.0.log
# Manual runs will write to <LOG_DIR>/<script-name>.<uid>.log
LOG_DIR=/Users/johndoe/workspace/dulce-jelly/scripts/backup/logs

# Mount script configuration
# Backup volume name (as it appears in /Volumes/)
VOL_NAME=MEDIA2

# Optional: Volume UUID (recommended if duplicate volume names exist)
# Get UUID with: diskutil info /Volumes/MEDIA2 | awk -F': *' '/Volume UUID/{print $2}'
VOLUME_UUID=

# Mount wait behavior (seconds)
MIN_WAIT_SEC=13
MAX_WAIT_SEC=60
POLL_SEC=1
