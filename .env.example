# Media Server Environment Variables
# Copy to .env and update with your values

LAN_HOSTNAME=mymini.local #your computer name on LAN eg: mac mDNS <servername>.local
PUBLIC_DOMAIN=my-jellyfin.me # a domain name you have purchased to host the server online
# Timezone & User
TZ=Asia/Tashkent
PUID=1000
PGID=1000

# Basic Auth for Public Access (dulcejelly.me)
# Username for basic auth on public subdomains
BASIC_AUTH_USER=admin
# Plain text password (will be hashed for Caddy)
BASIC_AUTH_PASS=changeme
# Bcrypt hash of password (generate: docker run --rm caddy:2-alpine caddy hash-password --plaintext 'yourpassword')
BASIC_AUTH_HASH=$2a$14$your_bcrypt_hash_here

# Test Credentials (used by test suite)
# Should match BASIC_AUTH_USER/PASS for auth tests to pass
TEST_AUTH_USER=admin
TEST_AUTH_PASS=changeme

# Service Admin Credentials
# Used for qBittorrent, etc.

# Jellyfin Auth Toggle (for internet access via dulcejelly.me)
# Set to "true" to require basic auth for Jellyfin at jellyfin.dulcejelly.me (may break some plugins)
# Set to "false" to allow unauthenticated access (Jellyfin has built-in auth)
# Default: false
JELLYFIN_AUTH_ENABLED=false

# Service Ports (defaults shown, override if needed)
CADDY_HTTP_PORT=80
CADDY_HTTPS_PORT=443
JELLYFIN_HTTP_PORT=3278
JELLYFIN_HTTPS_PORT=3279
JELLYSEERR_PORT=3277
PROWLARR_PORT=3276
RADARR_PORT=3273
SONARR_PORT=3272
QBITTORRENT_WEB_PORT=3275
QBITTORRENT_PEER_PORT=6881
SABNZBD_PORT=3274
FLARESOLVERR_LOG_LEVEL=info
HUNTARR_PORT=3271

# Jellyfin Media Paths
JELLYFIN_MOVIES=/Volumes/MEDIA1/Movies
JELLYFIN_SERIES=/Volumes/MEDIA1/Series

# Unified Downloads Root (recommended: use SSD for speed)
# All download clients (SABnzbd, qBittorrent) and automation tools (Radarr, Sonarr)
# share the same /downloads mount point for efficient file handling
DOWNLOADS_ROOT=/Volumes/SCRAPFS/MediaCenter/downloads
INCOMPLETE_ROOT=/Volumes/SCRAPFS/MediaCenter/downloads/incomplete

# Download Client Settings (configure these in the app UIs):
# - SABnzbd Completed Download Folder: /downloads/usenet
# - qBittorrent Save Path: /downloads/torrents
# - Radarr/Sonarr Download Client paths: /downloads/usenet (SAB) or /downloads/torrents (qBit)

# Optional: qBittorrent watch folder for .torrent files
QBITTORRENT_WATCH=~/Downloads

# Recyclarr Schedule
RECYCLARR_CRON=0 4 * * *
QUALITY_BROKER_CRON=0 0 * * *

# Misc
UMASK_SET=002
HOST_IP=127.0.0.1

# default true if unspecified
CADDY_AUTH_ENABLED=true

# Optional: for API automation against *arr
PROWLARR_API_KEY=

# Optional: Huntarr base URL when behind reverse proxy (example: /huntarr)
HUNTARR_BASE_URL=
